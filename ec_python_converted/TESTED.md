# Basic Test Result

已在当前环境完成两个最小运行测试：

```bash
python demo_OMELET.py --runs 1 --lambda-values 5 --gamma-values 5 --sigma-powers 0
python OMELET_SV.py --runs 1 --lambda-values 5 --gamma-values 5 --sigma-powers 0
```

两条命令均可正常输出 ACC/NMI/ARI/F-score 指标表。
